# Q3 并行改写存档

用户确认以 GitHub 提交 `51d74a17827152eae2a07cda84d85d1201bfb4c7` 的 Q3 为正文版本，将同期 Overleaf 整体改写另存备份，并保留最新摘要及其他独立修改。

这里原样保存 `6b98492` 的改写、随后 `99efc81` 的补图版本，以及再次出现的 `46aa285` 并行改写和它在 `2da6b2a` 中的图片路径修补稿，原文件路径均为 `sections/q3.tex`。来源提交和 SHA256 见 `q3_overleaf_backups.json`。文件采用 `.tex.txt` 后缀，未被论文入口引用。

用户随后同意在已确认正文上继续压缩第 6.6、6.8 节；上述备份只记录并行稿，不要求正式正文永远停留在最初确认提交。

这些文件用于追溯并行稿，不作为正式参数、数字或论证的依据。

2026-09-13，用户再次明确确认以四部分 Q3（问题分析、模型建立、模型求解、结果与结论）及附录为正式版本。新增原样备份 `f8cd33a` 的九小节并行改稿及 `8dac9cf` 的后续开头文字修订，来源与 SHA256 见 `q3_four_part_resolution.json`；正式正文以已验证的 `86ec9ed` 为准。独立的 Q2、数据说明及模型评价修改不因此回退。
