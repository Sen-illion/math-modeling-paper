# C283 摘要结构优化记录

- 日期：2026-09-13。工具：Codex（GPT-6 系列）、cumcm-award-paper-writer、Tectonic/XeTeX、PyMuPDF。
- 用户要求：参照 C283《双目标贪心优化下的企业原料订购运输方案》第 1 页优化摘要，使单页更充实；用户已确认推送，并明确选择 GitHub 的 Q3 版本处理同步冲突。
- 写作调整：采用“总述＋针对四问各一段＋关键词”；各问按任务、方法步骤、关键结果和效果判断展开。补充既有变量与约束、参数标定、实测执行和核验说明。
- 保持内容：小四字号、25 mm 页边距和论文标题不变；四问主要费用、购电量和降幅沿用正文已验证结果，模型、计算代码与结果工作簿均未改动。摘要完整排于首页。
- 冲突处理：按用户选择保留 GitHub `8851d89` 的完整 Q3 正文、配图引用和附录入口；接入 Overleaf 历史，其旧九节 Q3 不覆盖已确认的四部分结构。其他不冲突更新按正常合并保留。
- 参考范围：只借鉴摘要组织与方法、结果的叙述顺序，未采用参考论文的供应商模型、算法或数值。
- 核验：改写前已检查主要数字、摘要外文本及单页渲染；发布前再完整编译，复核关键词留在首页、正文下一页起排以及字体、页边距和内容边界。
- 人工环节：用户审定参照方向、同意推送，并明确选择 Q3 冲突保留版本；具体文字与编译检查由 Codex 完成。
- 发布前完整核验通过：摘要与用户确认预览逐像素一致；正文从第 2 页开始；Q3 与 `8851d89` 逐字一致且附录保留；未发现溢出、未定义引用、重复标签或缺字。同步接入的 Q2 改动仅为“调度策略对照与效果分析”小标题。

## 2026-09-13 摘要问题二比较金额勘误

- 用户已确认按核验结果将最终方案相对无分位方案的节省金额改为约 162.8 万元；此前摘要的 160.9 万元对应仅净分位的中间策略，Overleaf 暂改的 162.9 万元也与精确差值不符。
- 来源：建模仓库 `results/Q2/opt/full_year_summary_v1_samestack.json` 的 `total_cost=15323137.312363826`；正式 `results/Q2/opt/full_year_summary.json` 的 `total_cost=13695403.60698269`，后者 `n_validation_errors=0`。差值为 1627733.705381136 元，即 162.7733705381136 万元，保留一位小数为 162.8 万元。原模型、结果文件与 Q2 正文中间策略比较数字保持不变。
- 同步处理：合入 Overleaf `71e2575` 的新增附录代码，Q3 继续逐字保留用户选定的 GitHub `8851d89` 版本；附录 Q1 代码引用从 `../code/Q1/model_lp.py` 修正为论文仓库内实际路径 `code/Q1/model_lp.py`。
- 人工环节：用户确认数字勘误及继续同步；文字修正、结果差值复算、冲突处理与编译核验由 Codex 完成。
- 发布复核：完整 PDF 共 32 页，摘要单页且显示 162.8 万元；第 2–30 页与上次发布稿逐像素一致，Q3 正文、附录和接入代码核验一致。代码注释使用正文西文字体显示 η，代码块单倍行距避免末尾两行独占一页；未发现溢出、缺字、未定义引用或重复标签。

## 用户解决附录后重新同步

- 用户告知附录问题已解决并要求重新同步，接入 Overleaf `4fcc579` 的 8 份代码命名和附录引用；原始代码内容未修改。
- 保留已确认的扩充摘要与 162.8 万元勘误、GitHub 四部分 Q3 及其检验附录。Q4 对 Q3 公式的引用与所保留正文中的 `eq:q3_lp0`、`eq:q3_lp_adj` 标签对齐。
- 仅修复附录展示：文件名中的下划线作为普通字符显示，代码注释字体覆盖 η，代码块单倍行距；8 份代码全部保留。
- 同步前核验：完整 PDF 共 58 页（新增页为代码附录）；第 1–30 页与已核验的 162.8 万元版本逐像素一致，摘要单页，Q3 正文及检验附录与确认版本一致，8 份代码与 Overleaf 原文一致。未发现溢出、缺字、未定义引用或重复标签。
