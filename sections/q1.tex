\section{问题一：确定性日前购电计划}

\subsection{问题分析}

问题一要求根据已知的分时电价、小区负载和光伏预测功率，制定全天购电与储能充放电计划，在满足小区用电需求及储能运行要求的前提下，使微电网全天购电费用最低。由于能够利用储能设备实现不同时段之间的电能转移，若仅根据当前电价和供需情况逐时决策，将难以兼顾后续高价时段的用电需求。为此，本文制定计划购电策略时综合考虑全天电价、光伏发电量和负载。进一步考虑到购电费用关于购电量呈线性关系，电量平衡、储电量变化、储能容量及充放电功率等运行条件均可表示为线性等式或不等式，本文决定采用线性规划（Linear Programming, LP）建立优化模型，以高效统筹全天各时段的购电与储能安排，并在满足各项约束的条件下求得购电费用最小方案。

\subsection{模型建立}

为描述各时段的电能分配情况，记时段 $t$ 的计划购电量为 $G_t$，储能设备侧充电量和放电量分别为 $c_t$ 和 $d_t$，弃电量为 $w_t$，时段末储电量为 $E_t$，其中 $t=1,\ldots,144$。上述变量均以 $\mathrm{kWh}$ 为单位。

\subsubsection{目标函数}

问题一的优化目标为最小化全天计划购电费用。全天购电费用可表示为各时段购电费用之和，即
\begin{equation}
\min C=\sum_{t=1}^{144}\pi_t G_t.
\label{eq:q1_objective}
\end{equation}
其中，$C$ 为全天计划购电费用，$\pi_t$ 为时段 $t$ 的分时电价。

\subsubsection{约束条件}

\textbf{（1）时段电量平衡约束}

为保证每个时段的小区用电需求得到满足，需要维持微电网内部的电量平衡。记时段 $t$ 的小区负载功率和光伏预测功率分别为 $P_t^{\mathrm{load}}$ 和 $P_t^{\mathrm{pv}}$（kW），单个时段长度为 $\Delta t=1/6\,\mathrm{h}$；储能充放电效率记为 $\eta=0.9$。则每个时段的外网购电量、光伏发电量和储能有效放电量之和，应等于负载用电量、储能充电所需电量和弃电量之和，即
\begin{equation}
G_t+P_t^{\mathrm{pv}}\Delta t+\eta d_t
=
P_t^{\mathrm{load}}\Delta t+\frac{c_t}{\eta}+w_t,
\qquad t=1,\ldots,144.
\label{eq:q1_energy_balance}
\end{equation}

其中，$\eta d_t$ 表示储能向微网实际提供的电量，$c_t/\eta$ 表示微网为使储能设备增加 $c_t$ 电量所需提供的充电电量。

\textbf{（2）储电量状态转移约束}

储能设备能够将当前时段的电能保留至后续时段，因此前后时段的储电量需要连续变化。根据储能设备侧充放电量的定义，时段末储电量满足
\begin{equation}
E_t=E_{t-1}+c_t-d_t,
\qquad t=1,\ldots,144.
\label{eq:soc_transition}
\end{equation}

该式将前后时段的储能状态联系起来，使当前时段的充放电决策能够影响后续时段的可用储电量，为全天联合优化提供基础。

\textbf{（3）储能容量约束}

为保证储能设备始终在允许的容量范围内运行，根据附录给出的储能参数，对各时段末储电量设置上下限：
\begin{equation}
1200\le E_t\le10800,
\qquad t=1,\ldots,144.
\label{eq:q1_capacity}
\end{equation}

该约束避免储能设备出现过充或过放，使所得调度方案满足实际容量限制。

\textbf{（4）日初、日末储电量约束}

题目要求储能设备在 $0{:}00$ 和 $24{:}00$ 的储电量相同。根据给定的初始储电量，设置
\begin{equation}
E_0=E_{144}=6000.
\label{eq:soc_terminal}
\end{equation}

结合式~\eqref{eq:soc_transition} 对全天 $144$ 个时段求和，可进一步得到

\begin{equation}
\sum_{t=1}^{144}c_t
=
\sum_{t=1}^{144}d_t.
\label{eq:daily_energy_balance}
\end{equation}

该式表明，储能设备全天总充电量与总放电量相等，从而保证储能经过一天调度后恢复至初始储电水平。

\textbf{（5）接口充放电功率约束}
\begin{equation}
0\le\frac{c_t}{\eta}\le P_{\max}\Delta t,
\qquad
0\le\eta d_t\le P_{\max}\Delta t.
\label{eq:q1_power}
\end{equation}
代入 $\eta=0.9$、$P_{\max}=5000\,\mathrm{kW}$ 和 $\Delta t=1/6\,\mathrm{h}$，可得每个 $10\,\mathrm{min}$ 时段内储能设备侧充、放电量的上限分别为
\begin{equation}
0\le c_t\le750,
\qquad
0\le d_t\le925.9259.
\label{eq:q1_power_equiv}
\end{equation}

由此既满足功率限制，又正确计入储能充放电过程中的能量损耗，避免将 $5000\,\mathrm{kW}$ 的功率上限直接作用于储能设备内部储电量变化。

\textbf{（6）非负约束}

由于微电网不允许向外网售电，计划购电量不能取负值；当光伏发电超过当前负载及储能可接纳电量时，允许产生弃电。因此有
\begin{equation}
G_t\ge0,\qquad w_t\ge0,
\qquad t=1,\ldots,144.
\label{eq:q1_nonnegative}
\end{equation}

综上，以式~\eqref{eq:q1_objective} 为目标函数，并结合式~\eqref{eq:q1_energy_balance}--\eqref{eq:q1_nonnegative} 所示约束，即构成问题一的确定性线性规划模型。该模型通过储电量状态转移关系将全天各时段联系起来，在满足负载需求及储能运行条件的基础上，对外网购电与储能充放电进行联合优化。

\subsection{模型求解}

完成目标函数与各项约束的建立后，问题一已转化为一个确定性线性规划问题。本文首先将各决策变量及约束整理为标准线性规划形式，再采用 CBC 求解器搜索满足全部运行约束且购电费用最小的调度方案。具体求解过程如下：

\textbf{Step1：模型的矩阵化表示}

为将前述目标函数与约束条件统一纳入线性规划求解框架，将全天各时段的购电量、充电量、放电量、弃电量和储电量按类别排列，构成决策变量向量
\[
\boldsymbol{x}=
(G_1,\ldots,G_{144},
c_1,\ldots,c_{144},
d_1,\ldots,d_{144},
w_1,\ldots,w_{144},
E_1,\ldots,E_{144})^{\mathsf T}.
\]

其中，$\boldsymbol{x}$ 集中了全天 $144$ 个时段的所有待求变量，使各时段的购电与储能运行状态能够在同一优化模型中统一确定。

根据前文建立的目标函数、电量平衡关系及储能运行约束，可进一步将模型整理为标准线性规划形式：
\[
\begin{aligned}
\min_{\boldsymbol{x}}\quad
&\boldsymbol{p}^{\mathsf T}\boldsymbol{x},\\
\mathrm{s.t.}\quad
&A_{\mathrm{eq}}\boldsymbol{x}=\boldsymbol{b}_{\mathrm{eq}},\\
&A_{\mathrm{ub}}\boldsymbol{x}\le\boldsymbol{b}_{\mathrm{ub}},\\
&\boldsymbol{\ell}\le\boldsymbol{x}\le\boldsymbol{u}.
\end{aligned}
\]

其中，目标函数系数向量$\boldsymbol{p}$用于计算全天计划购电费用。由于只有外网购电量 $G_t$ 直接产生购电费用，因此 $G_t$ 对应的系数为各时段电价 $\pi_t$，其余变量对应的目标函数系数均为零。

等式约束矩阵$A_{\mathrm{eq}}$及其右端向量$\boldsymbol{b}_{\mathrm{eq}}$用于表示各时段的电量平衡、储电量状态转移以及日初日末储电量要求；不等式约束矩阵$A_{\mathrm{ub}}$及其右端向量$\boldsymbol{b}_{\mathrm{ub}}$用于表示接口充放电功率等运行限制；变量下界$\boldsymbol{\ell}$和上界$\boldsymbol{u}$则用于限制储电量、购电量、充放电量及弃电量的取值范围。

经过上述矩阵化处理，原模型中的目标函数和各类运行约束被统一表示为标准线性规划形式。

\textbf{Step2：CBC 求解与全天联合优化}

在标准线性规划模型的基础上，采用 CBC 求解器对全天 $144$ 个时段进行联合求解，在满足电量平衡、充放电效率、接口功率、储能容量及日末储电量等约束的条件下，以全天购电费用最小为目标，确定各时段的购电与储能运行方案。CBC 求解完成后，得到最优决策向量
\[
\boldsymbol{x}^{*}=
(G_1^{*},\ldots,G_{144}^{*},
c_1^{*},\ldots,c_{144}^{*},
d_1^{*},\ldots,d_{144}^{*},
w_1^{*},\ldots,w_{144}^{*},
E_1^{*},\ldots,E_{144}^{*})^{\mathsf T},
\]

其中包含全天各时段的最优购电量、充电量、放电量、弃电量及储电量。对应的最小全天计划购电费用为
\[
C^{*}=
\sum_{t=1}^{144}\pi_t G_t^{*}.
\]

\subsection{求解结果}

通过上述模型求解，得到全天计划购电策略与储能调度方案。为直观展示优化结果，表~\ref{tab:q1_purchase} 给出了指定时段的计划购电量及全天购电情况，表~\ref{tab:q1_storage} 给出了储能设备各时段的充放电量及日初、日末储电量。其中，全天计划购电量为 $59\,482.70$ kWh，全天购电费用为 $35\,126.85$ 元。

\begin{table}[htbp]
  \centering

  % ===== 左侧：购电结果 =====
  \begin{minipage}[t]{0.46\textwidth}
    \centering
    \small
    \setlength{\tabcolsep}{5pt}
    \caption{指定时段的计划购电量及全天购电量、购电费}
    \label{tab:q1_purchase}
    \begin{tabular}{lc}
      \toprule
      时间段 & 计划购电量（kWh）\\
      \midrule
      10:00--10:10 & 0.00\\
      12:00--12:10 & 486.40\\
      14:00--14:10 & 0.00\\
      16:00--16:10 & 394.93\\
      18:00--18:10 & 636.98\\
      20:00--20:10 & 0.00\\
      \midrule
      全天购电量（kWh） & $59\,482.70$\\
      全天购电费（元） & $35\,126.85$\\
      \bottomrule
    \end{tabular}
  \end{minipage}
  \hfill
  % ===== 右侧：储能结果 =====
  \begin{minipage}[t]{0.50\textwidth}
    \centering
    \small
    \setlength{\tabcolsep}{4pt}
    \caption{储能设备分时段充放电量及首末储能量}
    \label{tab:q1_storage}
    \begin{tabular}{lcc}
      \toprule
      时间段 & 充电量（kWh） & 放电量（kWh）\\
      \midrule
      0:00--4:00 & $4\,050.00$ & 0.00\\
      4:00--8:00 & 750.00 & $6\,608.24$\\
      8:00--12:00 & $3\,559.17$ & $2\,357.13$\\
      12:00--16:00 & $5\,507.43$ & 101.22\\
      16:00--20:00 & 0.00 & $5\,631.87$\\
      20:00--24:00 & $4\,800.00$ & $3\,968.13$\\
      \midrule
      0:00 储能量（kWh） & \multicolumn{2}{c}{$6\,000.00$}\\
      24:00 储能量（kWh） & \multicolumn{2}{c}{$6\,000.00$}\\
      \bottomrule
    \end{tabular}
  \end{minipage}
\end{table}

表~\ref{tab:q1_purchase} 中，10:00、14:00 和 20:00 的计划购电量均为 0，其中正午时段主要由光伏发电满足负载并为储能设备充电，20:00 则主要由储能设备放电满足负载。12:00 虽存在光伏富余，仍购电 $486.40\,\mathrm{kWh}$，主要由于此时电价处于低位，模型通过低价购电储能以满足后续高价时段的用电需求。由于 16:00 和 18:00 光伏发电量下降，需增加外网购电，外网购电量相对较多。

表~\ref{tab:q1_storage} 中，储能设备充电主要集中于夜间和正午低价时段，放电主要集中于早晚电价高峰，体现出明显的“低价充电、高价放电”规律。全天弃电量为 0，说明富余光伏发电量得到充分利用；$24{:}00$ 储电量恢复至 $6000\,\mathrm{kWh}$，满足储能设备在 $0{:}00$ 和 $24{:}00$ 的储电量相同的约束。

\subsection{模型检验}

将所得方案代回原模型，分别计算时段电量平衡残差与储电量递推残差：
\begin{equation}
r_t^{\mathrm{bal}}=G_t^{*}+V_t+\eta d_t^{*}-L_t-\frac{c_t^{*}}{\eta}-w_t^{*}.
\label{eq:balance_residual}
\end{equation}
\begin{equation}
r_t^{E}=E_t^{*}-E_{t-1}^{*}-c_t^{*}+d_t^{*}.
\label{eq:soc_residual}
\end{equation}

检查上述残差是否在数值容差内接近零，同时检查 $E_{144}^{*}=E_0$ 是否成立、各时段 $c_t^{*}d_t^{*}$ 是否接近零，以确认日内能量闭合并排查同时充放电。最后依据最优购电序列重新计算费用，检验目标值与调度方案的一致性，在相对容差 $10^{-4}$、绝对容差 $10^{-3}\,\mathrm{kWh}$ 下全部通过。

\begin{samepage}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.82\textwidth]{Q1_residuals_zh.png}
  \caption{电量平衡残差与储电量递推残差检验}
  \label{fig:Q1_residuals}
\end{figure}

\noindent
由图~\ref{fig:Q1_residuals} 可见，电量平衡残差与储电量递推残差均集中于零附近，且均处于绝对容差 $\pm 10^{-3}\,\mathrm{kWh}$ 范围内，表明所得调度方案满足电量平衡与储电量递推约束，模型求解结果具有较好的数值一致性。

\end{samepage}

\subsection{模型对比}
设置分时贪婪方案作为对照，该方案以日电价的33\%和67\%分位数划分低价与高价时段，在容量及功率限制内执行低价充电、高价放电，并恢复日末储电量。将线性规划方案与基线的购电费用、购电总量及弃光量进行比较，并以费用降幅评价经济性：
\begin{equation}
\rho=
\frac{C_{\mathrm{base}}-C^{*}}
     {C_{\mathrm{base}}}
\times100\%.
\label{eq:cost_reduction}
\end{equation}

其中，$C_{\mathrm{base}}$ 为分时贪婪方案的全天购电费用，
$C^{*}$ 为线性规划最优方案的全天购电费用。

\begin{table}[htbp]
  \centering
  \caption{不同调度方案的结果比较}
  \label{tab:baseline_comparison}
  \small
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{lccc}
    \toprule
    方案 & 全天购电量/kWh & 全天购电费用/元 & 弃电量/kWh \\
    \midrule
    分时贪婪 & $62\,581.85$ & $38\,870.13$ & $3\,628.93$ \\
    线性规划 & $59\,482.70$ & $35\,126.85$ & 0.00 \\
    \bottomrule
  \end{tabular}
\end{table}

由表~\ref{tab:baseline_comparison} 可见，线性规划方案在购电量、购电费用和弃电量三个指标上均优于分时贪婪方案。其中，全天购电量由 $62\,581.85$ kWh 降至 $59\,482.70$ kWh，弃电量由 $3\,628.93$ kWh 降至 0，全天购电费用减少 $3\,743.28$ 元，降幅为 $9.63\%$。这表明线性规划能够统筹各时段的购电、光伏利用与储能充放电，在降低购电成本的同时提高富余光伏的利用程度。