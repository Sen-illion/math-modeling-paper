\section{问题二：日前预测购电与日内滚动回测模型}

\subsection{问题分析}

问题二在问题一的线性规划上加入真实的不确定性。每天电价仍取附件~1 的同一套 $144$ 点分时电价 $\pi_t$，但小区负载与光伏按附件~2 逐日变化；每天 $0{:}00$ 制定当天计划购电时，只能使用该日以前的历史，当天真值未知。计划购电量 $G_t$ 一旦锁死，日内供电不足须按交易时刻电价的 $5$ 倍紧急购电，其余费用仍按计划量结算。因此本问是日前决策、日内执行的两阶段问题：缺口按 $5\pi$ 结算、多余计划电仍按 $\pi$ 结算，少购代价重于多购，后文按这一不对称代价取净负载分位，控制计划缺口。

相对问题一，本问继承电量平衡、储电量窗 $[1200,10800]$\,kWh、接口 $5000$\,kW、效率口径、不可倒送与允许弃光；新机制是信息不完全、跨日传递实际储电量、不再强制每日 $6000$\,kWh 闭环。正式评价窗口为 $2025$ 年 $2$ 月 $1$ 日至 $12$ 月 $31$ 日（共 $334$ 天）。由模型假设，$2$ 月 $1$ 日 $0{:}00$ 储电量再置为 $6000$\,kWh，$1$ 月只用于预报训练与残差分位。若改为从 $1$ 月 $1$ 日的 $6000$\,kWh 滚入 $2$ 月 $1$ 日（初值约 $3018$\,kWh），全年约多 $1419$ 元，结论方向不变。

据此，本问按四段求解：用附件~2 的历史负载与光伏分别做点预测，将净负载分位送入日前线性规划，锁死计划购电后再按当天真值做保护回放，总费用为计划费加 $5$ 倍紧急费。保护回放是指：日内用真实负载与光伏执行已锁死的计划，放电时按比例 $\rho$ 预留日前计划储电量，不一次放到下限，缺口记紧急购电。日前规划约束同问题一，并增加日末库存。负载用 XGBoost 而不用周相似，是因为用电受作息支配，工作日与周末峰值形态不同，且依赖近几日水平，仅复制上周同一天会丢掉日历与近期信息；光伏没有人为周周期，由日照与近几日天气决定，故用近几日同刻均值，夜间无日照时段置零，不为光伏套用负载那套星期特征。分位、日末库存与保护回放的参数取值见后文，不在 $2$--$12$ 月正式窗上用总费用反调。

\subsection{模型建立}

\subsubsection{决策变量与参数}

对第 $D$ 日第 $t$ 个时段（$t=1,2,\dots,144$）定义决策变量：$G_t$ 为计划购电量，$c_t$ 为微网侧充电取电量，$d_t$ 为微网侧放电供电量，$w_t$ 为弃光电量，$E_t$ 为时段末储电量，单位均为 kWh。电价 $\pi_t$ 的单位为元/kWh，负载与光伏功率 $P^{\mathrm{load}}_{t}$、$P^{\mathrm{pv}}_{t}$ 的单位为 kW，$\eta=0.9$，$E_{\min}=1200$、$E_{\max}=10800$（kWh），$P_{\max}=5000$\,kW，$\Delta t=1/6$\,h，故每时段接口上限为 $P_{\max}\Delta t\approx833.33$\,kWh。

本问 $c_t$、$d_t$ 按模型假设记在微网交流侧，与问题一储能设备侧由 $\eta$ 换算：充电时母线取电 $c_t$、储能设备增加 $\eta c_t$；放电时母线供电 $d_t$、储能设备减少 $d_t/\eta$。接口约束因此直接写成 $c_t,d_t\le P_{\max}\Delta t$。

\subsubsection{XGBoost 训练与预测}

XGBoost 是梯度提升树\cite{chen2016xgboost}：依次添加回归树拟合前一轮残差，把表格特征映射为连续预测。本文用它估计当天 $144$ 个时段的负载，是因为它能刻画星期与时刻的非线性交互，且每天扩样本后可以重训。下文只固定本题的样本、训练窗与超参数。

第 $D$ 日 $0{:}00$ 的信息集严格限定为日期 $<D$ 的附件~2，不含当天及未来任何真值。日历特征（时段序号、星期、月份、年内日序）在 $0{:}00$ 已知，可以使用。

\textbf{（1）样本构造。} 以单个（日，时段）为一条训练样本。对历史日 $d=0,\ldots,D-1$ 的时段 $s=0,\ldots,143$，特征为
\[
\bigl(\mathrm{slot},\;\mathrm{dow},\;\mathrm{month},\;\mathrm{doy},\;
\mathrm{lag}_{1\mathrm{d}},\;\mathrm{lag}_{2\mathrm{d}},\;\mathrm{lag}_{7\mathrm{d}},\;
\mathrm{mean}_{3\mathrm{d}},\;\mathrm{mean}_{7\mathrm{d}},\;\mathrm{mean}_{30\mathrm{d}},\;
\mathrm{std}_{7\mathrm{d}}\bigr),
\]
其中 $\mathrm{slot}=s$ 为时段序号，$\mathrm{dow}$ 为星期，$\mathrm{month}$ 为月份，$\mathrm{doy}$ 为年内日序；三个滞后与滑动均值、标准差全部只使用日期 $<d$ 的同刻负载，滑动窗不足时用已有天数。标签 $y$ 为第 $d$ 日第 $s$ 时段的实测负载（kW）。因此第 $D$ 日 $0{:}00$ 的训练集共有 $D\times144$ 行，且没有任何一行含有第 $D$ 日实测。

\textbf{（2）扩展窗重训。} 采用 expanding：第 $D$ 天的训练集从 $1$ 月 $1$ 日一直扩到第 $D-1$ 日，每天 $0{:}00$ 用扩了一天的新样本重新拟合一棵树，而不是把同一模型沿用多日。这样冬夏样本会随日历进入训练集，避免滚动窗把早期季节丢掉。若历史不足 $144$ 行（尚不满一天），该日负载退回过去 $7$ 日同刻均值，以免空窗拟合。先让负载与光伏用同一套预测、只换训练窗，用来确定负载骨干：二者都用 $7$ 日均值时全年约 $2824$ 万元，都用 XGB 扩展窗时约 $1533$ 万元（图~\ref{fig:q2_model_compare} 前四组），故负载取扩展窗。这还不是正式方案；正式方案是负载用扩展窗、光伏用 $7$ 日均值。

\begin{figure}[H]
  \centering
  \makebox[\textwidth][c]{\includegraphics[width=0.88\textwidth]{Q2_fig3_model_compare.png}}
  \caption{不同预测与控制策略的全年总购电费比较}
  \label{fig:q2_model_compare}
\end{figure}

由图~\ref{fig:q2_model_compare} 可见：前四组把负载和光伏绑成同一种预测，只比较训练窗，扩展窗最低；最上面一根是负载也用 $7$ 日均值，所以贵，并不表示光伏不该用 $7$ 日均值。后两组才是正式预测组合（负载 XGB 扩展窗 $+$ 光伏 $7$ 日均值），再比较控制层：固定分位 $0.8$ 为 $1376.52$ 万元，正式净分位加日末 $2400$ 与 $\rho$ 为 $1369.54$ 万元。前四组 $2$ 月 $1$ 日储电量由 $1$ 月回测滚入，后两组置为 $6000$\,kWh，两段费用不宜直接相减。

\textbf{（3）拟合目标与超参数。} 树模型只估计当天各时段负载的均值，故拟合目标取平方误差；少买按 $5\pi$ 结算的风险不写入树，而由后文净负载分位处理。每天 $0{:}00$ 都要在扩大后的样本上重训，故取较浅的树和较小的学习率，具体为树的数量 $200$，最大深度 $6$，学习率 $0.05$，行、列采样均为 $0.8$，叶子最小权重 $4$，$L_2$ 正则系数 $1.0$，用直方图方法寻找分裂点，随机种子 $42$。

\textbf{（4）当天预测。} 用第 $D$ 日的 $144$ 行特征（日历取当天，滞后与滑动量仍只含 $D$ 日之前）一次给出当天 $144$ 个时段的负载点预测 $\hat P^{\mathrm{load}}_{t}$，再截断为非负。特征中不含当天实测负载或光伏，泄漏检查覆盖特征因果性、训练窗边界与“同日/未来数据免疫”，探测日全部通过（错误 $0$ 条）。正式窗负载点预测 MAE 为 $136.42$\,kW。

光伏不采用 XGBoost，是因为光伏与负载不是同一类过程。二者逐时段 Pearson 相关全年均值仅 $0.031$，应当分开建模；光伏相邻日总电量自相关高达 $0.964$，由日内日照曲线和近几日天气决定，没有负载那种人为星期周期。若把为负载准备的、含星期特征的树拿来拟合光伏，会把工作日噪声写进正午出力，并把大量夜间零点与白天尖峰塞进同一个平方误差。

因此光伏用近 $7$ 日同一时刻的均值作为当天该时刻的点预测，并对历史同刻恒为 $0$ 的夜间时段强制置零：
\begin{equation}
\hat P^{\mathrm{pv}}_{t}=\frac{1}{7}\sum_{k=1}^{7}P^{\mathrm{pv}}_{D-k,t},\qquad t=1,\dots,144. \label{eq:q2_pv_forecast}
\end{equation}
作为印证，点预测 MAE 上 $7$ 日均值（$153.40$\,kW）低于同一套 XGBoost（$159.33$\,kW）。该对照只比较光伏点预测误差，与图~\ref{fig:q2_model_compare} 前四组的全年购电费不是同一件事：前四组最贵的一根是负载也改用 $7$ 日均值。

\subsubsection{净负载分位与日前线性规划}

点预测不能表达误差方向。题面只给出：少买 $1$\,kWh 须按紧急电价 $5\pi$ 补购，多买 $1$\,kWh 仍按计划电价 $\pi$ 结算。将前者记为缺货成本 $C_u$、后者记为超储成本 $C_o$，则临界覆盖率为
\begin{equation}
\frac{C_u}{C_u+C_o}=\frac{5\pi}{5\pi+\pi}=0.8.
\label{eq:q2_critical}
\end{equation}
因此不对负载、光伏分别取分位，而在净负载上做保守修正。记实测净负载、点预测净负载与残差为
\begin{equation}
\begin{aligned}
N_t&=P^{\mathrm{load}}_{t}-P^{\mathrm{pv}}_{t},\\
\hat N_t&=\hat P^{\mathrm{load}}_{t}-\hat P^{\mathrm{pv}}_{t},\\
r^N_t&=N_t-\hat N_t,
\end{aligned}
\label{eq:q2_netdef}
\end{equation}
其中残差只含日期 $<D$。取 expanding 残差池的 $0.8$ 分位
\begin{equation}
\tilde N_t=\hat N_t+Q_{0.8}(r^N_{\cdot,t}).
\label{eq:q2_net_quantile}
\end{equation}
送入日前 LP 时，光伏点预测保持不变（夜间仍归零），把保守净负载加回负载
\begin{equation}
\tilde P^{\mathrm{load}}_{t}=\max\bigl(\hat P^{\mathrm{pv}}_{t}+\tilde N_t,0\bigr).
\label{eq:q2_load_back}
\end{equation}
这样计划覆盖的是偏高的净缺口，而不是把负荷上偏、光伏下偏拆成两次平移。

以修正后的预测覆盖全天净负载，在 $0{:}00$ 求解日前线性规划：
\begin{equation}
\begin{aligned}
\min\quad &\sum_{t=1}^{144}\pi_t G_t \\
\mathrm{s.t.}\quad
&G_t+\tilde P^{\mathrm{pv}}_{t}\Delta t+d_t=\tilde P^{\mathrm{load}}_{t}\Delta t+c_t+w_t,\\
&E_t=E_{t-1}+\eta c_t-d_t/\eta,\\
&E_{\min}\le E_t\le E_{\max},\\
&0\le c_t,d_t\le P_{\max}\Delta t,\qquad G_t,w_t\ge0,\\
&E_{144}=S^{*}=2400\,\mathrm{kWh},
\end{aligned}
\label{eq:q2_lp}
\end{equation}
其中 $E_0$ 取上一日实际执行末值（$2$ 月 $1$ 日为 $6000$\,kWh）。日末 $2400$\,kWh 只约束计划轨迹，实际执行并不重置到该值。日前 LP 只锁死计划购电 $G_t$，充放电按日内回放重算，不作为硬约束。

\subsubsection{日内 $\rho$ 保护回放}

保护是指缺口到来时不把储能一次放到下限，回放是指计划购电锁死后用当天真值逐时段执行。记净残差
\begin{equation}
r_t=P^{\mathrm{load}}_{t}\Delta t-P^{\mathrm{pv}}_{t}\Delta t-G_t.
\label{eq:q2_resid}
\end{equation}
若 $r_t\le0$，仍按贪心充电、剩余弃光，紧急电不用于充电。若 $r_t>0$，并不把储能设备放到 $E_{\min}$，而是先按日前计划储电量构造预留线
\begin{equation}
\underline{E}_t=E_{\min}+\rho\bigl(E^{\mathrm{plan}}_{t}-E_{\min}\bigr),
\label{eq:q2_reserve}
\end{equation}
放电上限为
\begin{equation}
\min\bigl(r_t,\;\eta(E_{t-1}-\underline{E}_t),\;P_{\max}\Delta t\bigr),
\label{eq:q2_dmax}
\end{equation}
其余记紧急购电 $e_t$，按 $5\pi_t$ 结算。式中 $\rho\in[0,1]$ 是预留比例：$\rho=0$ 放到下限 $1200$\,kWh，$\rho=1$ 则完全按日前计划储电量放电，越大越不准提前把计划库存放完。正式取 $\rho=0.625$。全天总费用为
\begin{equation}
C=\sum_{t}\pi_t G_t+\sum_{t}5\pi_t e_t,
\label{eq:q2_cost}
\end{equation}
回放末值 $E_{144}$ 作为次日实际 $E_0$。

\subsubsection{参数取值与依据}

\begin{enumerate}
  \item \textbf{XGB 超参数}树深取 $6$、学习率取 $0.05$（并配 $200$ 棵树、行列采样 $0.8$）：每天都要从很少的历史扩到全年重训，浅树慢学习避免早期过拟合、后期震荡。
  \item \textbf{光伏用 $7$ 日同刻均值}因为无星期周期、相邻日自相关高，不是按正式窗总费用搜出来的；MAE 仅作印证。
  \item \textbf{净分位 $\alpha=0.8$}由上述临界覆盖率 $5/(5+1)$ 预指定，不是在 $2$--$12$ 月上扫出来的。残差池按模型假设用 expanding 全历史；改成最近 $7$ 天全年更贵，按季拆分见检验。
  \item \textbf{$S^{*}=2400$ 与 $\rho=0.625$}只在 $2025$ 年 $1$ 月 $15$--$31$ 日的 $16$ 格上选定。网格为 $\alpha\in\{0.8,0.825\}$、$\rho\in\{0.5,0.625\}$、$S^{*}\in\{2400,3600,4800,6000\}$，从储电量 $6000$\,kWh 起做反事实回放、按库存折算现金打分；expanding 残差与 $W{=}7$ 都指向同一组 $(0.8,0.625,2400)$。$\alpha=0.825$ 在本预测器上全年更厚、更贵，未采用。$2$ 月 $1$ 日之后这三项全部冻结。该 $1$ 月窗口与正式评价窗同属 $2025$ 年，不是独立年份，因此不是真样本外；只是没有使用 $2$--$12$ 月正式窗来写规则，比看过 $7$--$12$ 月再定参更干净。
\end{enumerate}

\subsection{模型求解与结果}

每天 $0{:}00$ 重训 XGB 并求解一次含 $144\times5$ 个连续变量的线性规划，求解器为 CBC，正式窗全部返回 Optimal。全年（$2025$ 年 $2$ 月 $1$ 日至 $12$ 月 $31$ 日）正式结果为：总购电费用 $\textbf{13\,695\,403.61}$ 元，其中计划购电费 $13\,184\,604.50$ 元、紧急购电费 $510\,799.11$ 元；紧急购电量 $87\,331.34$\,kWh，分布在 $137$ 天共 $825$ 个时段；全年计划购电量 $21\,521\,947.29$\,kWh，弃光量 $2\,472\,688.96$\,kWh。实际储电量全程位于 $[1200,10800]$\,kWh（最低触及 $1200$，最高触及 $10800$），$2$ 月 $1$ 日初值 $6000$\,kWh，期末实际 $2678.35$\,kWh（计划日末均为 $2400$），无同时充放电、无约束校验错误。表~\ref{tab:q2_purchase}--表~\ref{tab:q2_emergency} 给出题目指定日期的计划购电、储能充放电与紧急购电。

\begin{table}[htbp]
  \centering
  \small
  \setlength{\tabcolsep}{4pt}
  \caption{问题二：指定日期计划购电量及全天购电费——冬季日购电量显著高于夏季（kWh / 元）}
  \label{tab:q2_purchase}
  \begin{tabular}{lcccc}
    \toprule
    时间段 & 2025.3.20 & 2025.6.21 & 2025.9.23 & 2025.12.21 \\
    \midrule
    10:00--10:10 & 0.00 & 0.00 & 0.00 & 0.00 \\
    12:00--12:10 & 641.27 & 0.00 & 554.73 & 1052.69 \\
    14:00--14:10 & 0.00 & 0.00 & 0.00 & 0.00 \\
    16:00--16:10 & 466.08 & 127.67 & 475.19 & 841.17 \\
    18:00--18:10 & 743.26 & 437.62 & 806.06 & 739.53 \\
    20:00--20:10 & 0.00 & 0.00 & 0.00 & 0.00 \\
    \midrule
    全天计划购电量(kWh) & 70698.38 & 34922.73 & 69093.42 & 99124.96 \\
    全天计划购电费(元) & 43184.03 & 20252.71 & 42877.32 & 64123.11 \\
    紧急购电费(元) & 0.00 & 0.00 & 1933.20 & 0.00 \\
    全天总购电费(元) & 43184.03 & 20252.71 & 44810.52 & 64123.11 \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{table}[htbp]
  \centering
  \small
  \setlength{\tabcolsep}{4pt}
  \caption{问题二：指定日期储能充放电量及起止储电量——凌晨低价充电、早晚高峰放电（微网侧，kWh）}
  \label{tab:q2_storage}
  \begin{tabular}{lcccccccc}
    \toprule
    时间段 & \multicolumn{2}{c}{2025.3.20} & \multicolumn{2}{c}{2025.6.21} & \multicolumn{2}{c}{2025.9.23} & \multicolumn{2}{c}{2025.12.21} \\
    & 充电量 & 放电量 & 充电量 & 放电量 & 充电量 & 放电量 & 充电量 & 放电量 \\
    \midrule
    0:00--4:00 & 7718.29 & 49.42 & 456.07 & 118.42 & 7992.53 & 0.00 & 7847.31 & 144.43 \\
    4:00--8:00 & 969.00 & 4426.56 & 542.27 & 1735.80 & 722.97 & 5767.24 & 1521.31 & 667.36 \\
    8:00--12:00 & 5458.12 & 3265.56 & 9472.90 & 0.00 & 4774.60 & 2387.94 & 5135.59 & 7756.25 \\
    12:00--16:00 & 4053.33 & 266.64 & 0.00 & 0.00 & 5581.23 & 530.20 & 7180.91 & 2220.11 \\
    16:00--20:00 & 111.19 & 4859.30 & 89.44 & 5164.11 & 123.97 & 4997.03 & 0.00 & 4779.93 \\
    20:00--24:00 & 1815.79 & 3681.54 & 2268.71 & 3017.50 & 1948.30 & 3517.84 & 1929.15 & 3622.79 \\
    \midrule
    $0{:}00$ 储电量(kWh) & \multicolumn{2}{c}{3308.16} & \multicolumn{2}{c}{3436.12} & \multicolumn{2}{c}{2993.40} & \multicolumn{2}{c}{3270.22} \\
    $24{:}00$ 储电量(kWh) & \multicolumn{2}{c}{3033.50} & \multicolumn{2}{c}{3831.65} & \multicolumn{2}{c}{2911.26} & \multicolumn{2}{c}{3199.88} \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{table}[htbp]
  \centering
  \small
  \setlength{\tabcolsep}{4pt}
  \caption{问题二：指定日期紧急购电明细——仅 $9$ 月 $23$ 日晚高峰出现缺口（kWh）}
  \label{tab:q2_emergency}
  \begin{tabular}{lcc}
    \toprule
    日期 & 购电时间段 & 购电量(kWh) \\
    \midrule
    2025/9/23 & 20:30--20:50 & 254.00 \\
    2025/9/23 & 21:00--21:10 & 34.88 \\
    \bottomrule
  \end{tabular}
\end{table}

由表~\ref{tab:q2_purchase} 至表~\ref{tab:q2_emergency} 可见：计划购电集中在光伏回落的 $16{:}00$、$18{:}00$ 晚高峰，$10{:}00$ 与 $20{:}00$ 分别由富余光伏与储能放电覆盖；储能仍是“凌晨低价充电、早晚高峰放电”，但 $24{:}00$ 储电量不再钉在 $6000$，而是在运行窗内滚动，四天日末均高于计划硬约束 $2400$。指定四天中仅 $9$ 月 $23$ 日出现紧急购电，且集中在 $20{:}30$ 之后的晚高峰尾段。

为展示信息不完全条件下一日调度的时段结构，将指定日 2025-03-20 的 $144$ 个 $10\,\mathrm{min}$ 时段画成环形视图（图~\ref{fig:q2-circular}）。图沿顺时针自 $00{:}00$ 起读，由外向内依次为：分时电价色带、净负载环、充放电调度环以及储电量 SOC 轨迹。电价取附件 1 典型日分时电价，负载与光伏取附件 2 当日实测，计划购电与充放电、SOC 均来自正式策略的日前锁死量与 $\rho$ 因果回放。该日负载 $117.82\,\mathrm{MWh}$、光伏 $55.44\,\mathrm{MWh}$、净缺口 $62.38\,\mathrm{MWh}$；计划购电 $70\,698.38$ kWh，购电费 $43\,184.03$ 元，紧急购电为 $0$。

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.78\textwidth]{q2_circular_dispatch.pdf}
  \caption{指定日 2025-03-20：夜间低价补能、早晚高峰放电，SOC 触及 $10800\,\mathrm{kWh}$ 上限且全日无紧急购电。}
  \label{fig:q2-circular}
\end{figure}

由图~\ref{fig:q2-circular} 可得出四点：
\begin{enumerate}
  \item 调度与电价、净负载同相位，而非按 $6000\,\mathrm{kWh}$ 做日闭环：SOC 在 $[1200,10800]\,\mathrm{kWh}$ 内随电价滚动，日初 $3308.16\,\mathrm{kWh}$、日末 $3033.50\,\mathrm{kWh}$ 交还次日。
  \item 谷充峰放成立、储能容量被充分使用：$0{:}00$--$4{:}00$ 充电 $7718.29\,\mathrm{kWh}$ 并在低价段触顶；$4{:}00$--$8{:}00$ 与 $16{:}00$--$20{:}00$ 分别放电 $4426.56$ / $4859.30\,\mathrm{kWh}$，SOC 降至全日最低 $1427.02\,\mathrm{kWh}$。
  \item 信息不完全下，保守日前计划叠加 $\rho$ 回放使当日紧急购电为 $0$。
  \item 容量上限是消纳损失来源之一：SOC 触顶导致弃光 $4.74\,\mathrm{MWh}$，此外无紧急电、无缺供。
\end{enumerate}

\subsection{模型检验}

\subsubsection{控制层消融}

在同一套点预测（XGB 扩展窗负载 $+7$ 日光伏）和 $2$ 月 $1$ 日 SOC$_0=6000$\,kWh 下，把净分位、日末硬约束与 $\rho$ 逐项打开，见表~\ref{tab:q2_ablation}。

\begin{table}[htbp]
  \centering
  \small
  \setlength{\tabcolsep}{4pt}
  \caption{问题二控制层消融：日末 $2400$ 是大头，$\rho$ 再省约三千元且期末库存更高}
  \label{tab:q2_ablation}
  \begin{tabular}{lcccc}
    \toprule
    策略 & 计划购电费(元) & 紧急购电费(元) & 紧急电量(kWh) & 总购电费(元) \\
    \midrule
    无分位（第一版骨干） & 12124833.88 & 3198303.43 & 525856.18 & 15323137.31 \\
    固定分量 $0.8/0.2$ & 13369773.46 & 395394.12 & 66910.43 & 13765167.58 \\
    净分位 $\alpha{=}0.8$ & 13192124.98 & 522427.80 & 87329.02 & 13714552.77 \\
    再加 $S^{*}{=}2400$ & 13185526.68 & 513021.95 & 82852.85 & 13698548.63 \\
    再加 $\rho{=}0.625$（正式） & 13184604.50 & 510799.11 & 87331.34 & 13695403.61 \\
    \bottomrule
  \end{tabular}
\end{table}

由表~\ref{tab:q2_ablation} 可见：相对固定分量 $0.8/0.2$，改成净负载分位省 $50\,615$ 元，再加上日末 $2400$ 又省约 $1.6$ 万元，最后加 $\rho$ 只再省约 $3\,145$ 元；正式策略全年较固定分量净省 $\textbf{69\,764}$ 元（$0.51\%$）。紧急电量与天数高于固定分量，但是用略多的计划购电换掉了第一版超过 $300$ 万元的紧急费。期末实际储电量 $2678$\,kWh，高于固定分量的 $1473$\,kWh，说明省费不是靠把库存吃光。完美信息下界为 $12\,251\,652.83$ 元，与正式值相差约 $12.4\%$，余量来自信息而非约束违反。

\subsubsection{可行性与残差池}

预测与残差分位只用日期 $<D$ 的数据。全年仿真无同时充放电、储电量始终落在 $[1200,10800]$\,kWh、校验错误 $0$ 条，回放供电缺口不超过容差。残差按气象季拆分后，在固定分量 $0.8$ 下全年约贵 $21.9$ 万元，故仍用全历史 expanding；「全历史代表明天」仍是强假设，只是按季分层并未更好。
